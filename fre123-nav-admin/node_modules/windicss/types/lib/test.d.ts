import type { Processor } from './index';
export declare function testStatic(processor: Processor, className: string): boolean;
export default function test(processor: Processor, className: string, prefix?: string): boolean;
